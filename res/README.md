This directory is intentionally kept clean in the research-ready package.

Use:

```bash
bash ../run_all_scenarios.sh
```

to generate fresh CSV outputs and summary plots.

The analysis helpers are:
- `analyze_results.py`
- `analyze_results1.py`
